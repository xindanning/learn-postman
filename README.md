## 项目介绍

接口测试demo

node: express 4.x

## 项目启动

进入项目路径并安装依赖

```html
npm install --registry=http://registry.npm.taobao.org
```

启动项目

```
# 本地开发则启动
npm run start
```

如果是本地开发则最后输入 `http://localhost:8114/classes/getList` 访问。
